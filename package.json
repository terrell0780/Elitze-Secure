{
  "name": "@elitze/secure",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "@types/node": "^22.13.0"
  }
}